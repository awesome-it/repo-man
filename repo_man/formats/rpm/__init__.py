"""RPM format: pull-through cache and publish."""

# Backend is imported by repo_man.formats.registry to avoid pulling in httpx
# when only version or other submodules are used.
__all__: list[str] = []
